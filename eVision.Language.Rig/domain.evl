domain eVision

define descriptor StandardRiskLevel with
	item LowLow
	item Low
	item High

define descriptor Variant with
	item ConfinedSpaceWork with translation for fr-fr as "Purlevu la France"


define descriptor RiskLevel based on StandardRiskLevel with
	item SuperHigh with rank as 11